// flags.ts

// Define tus flags aquí
const flags = {
    newFeature: true, // Ejemplo de una nueva característica habilitada
    featureX: false, // Ejemplo de una característica deshabilitada
    // Agrega más flags según sea necesario
  };
  
  export default flags;